; generated by drom from package skeleton 'library'

(library
  (name invaders_lib)
  (public_name invaders_lib)
  (wrapped true)
  (libraries notty notty.lwt lwt lwt.unix )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package invaders_lib))


